---
title: <功能名稱> — 實作計畫
domain: <domain>
feature: <feature>
spec: ./spec.md        # 來源 spec；無 spec 來源時填寫實際依據
status: draft          # draft | in-progress | done
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
---

# <功能名稱> — 實作計畫

## 前置確認

<開工前必須成立的事實。這節是整份計畫的地基，讀者靠它判斷任務為什麼這樣排。>

- **來源 spec**：[spec.md](./spec.md) — 本次涵蓋 <FR-1、FR-2…>；本次不做 <FR-N（理由）>
- **參考實作**：`<path/to/similar>` — <為什麼照它：同類型流程／同層命名慣例>
- **驗證指令**：測試 `<cmd>`｜lint `<cmd>`｜build `<cmd>`
- **可複用**：<已存在、不需重造的東西；沒有就寫「無」>
- **前提**：<不需我方動作、但必須成立的外部條件，如某個服務已上線、某個 API key 已核發。需要有人動手才會成立的事不寫在這裡——升格成 T0>

## 任務

<每個任務是一次能 commit 的量，且都有可執行的 verify。>

### T0 — <讓 repo 能開工的前置動作；沒有這種事就刪掉整節>

- **產出**：<例如：`go mod tidy` 後 build 可通過／缺的設定檔已補齊>
- **檔案**：`<受影響的檔案>`
- **相依**：無
- **verify**：`<可執行指令>` 通過

### T1 — <一句話講清楚這個任務要達成什麼>

- **產出**：<可觀察的成果，不是「開始做 X」而是「X 可以被呼叫並回傳 Y」>
- **檔案**：`<path>`（新增）、`<path>`（修改：<改什麼>）
- **相依**：無
- **verify**：`<可執行指令>` 通過，且 <具體可觀察結果>
- **備註**：<只在有非顯而易見的技術取捨時寫：選了什麼、為什麼。沒有就刪掉這行>

### T2 — <…>

- **產出**：
- **檔案**：
- **相依**：T1
- **verify**：

### T3 — <…>

- **產出**：
- **檔案**：
- **相依**：T1（可與 T2 平行）
- **verify**：

## 執行順序

<用最短的形式表達相依與可平行處。任務少時用清單，分支多時用 mermaid。>

```
T1 ─┬─→ T2 ─→ T4
    └─→ T3 ─┘
```

## 驗收對照

<每個本次要做的需求都要有任務對應；不做的也要留一列，明確標示。>

| 需求 | 任務 | 驗收標準（引自 spec） | 狀態 |
|------|------|----------------------|------|
| FR-1 | T1, T2 | Given <前提>，When <操作>，Then <預期結果> | [ ] |
| FR-2 | T3 | … | [ ] |
| FR-3 | — | — | 本次不做（<理由>） |

## 風險與未定

<會擋住執行或讓計畫失準的事。誠實標記，不要假裝已解決。>

- **風險**：<風險描述> → <緩解或偵測方式>
- [ ] `[待確認]` <未決事項> — 擋住 <T?>
