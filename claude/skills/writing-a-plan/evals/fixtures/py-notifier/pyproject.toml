[project]
name = "notifier"
version = "0.4.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27", "jinja2>=3.1", "sqlalchemy>=2.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: 需要外部服務，預設不跑"]
addopts = "-m 'not integration'"

[tool.mypy]
strict = true
