<!--
Default session-handoff scaffold, used by the session-handoff skill when a project
has NO handoff convention of its own. Copy this, fill every section, redact secrets,
and reference artifacts (commits / PRs / plans) by path or URL instead of pasting them.
When a project DOES define its own handoff format, use that instead of this file.
-->

---
branch: <git branch, if in a repo>
updated: <YYYY-MM-DD>
---

# Session Handoff

## Verified now
- What works + the exact check that proved it

## Changed this session
- Code/behavior added (reference commits/PRs by path — don't paste them)

## Broken or unverified
- Known defects, untested paths, risk for the next session

## Next best step
- The single highest-value next action
- Why it's next · what counts as done · what must NOT change

## Commands
- Startup · verify · focused-debug

## Suggested skills
- Skills the next agent should invoke first
